//My todos should have a way to delete todos
// we make use of the splice function
// we need to know the location of the item we want to delete in the array

todos.splice(); // we pass in 2 arguments, one for the location of the item

todos = ['item 1','item 2','item 3','item 4',' item 5'];
todos.splice(0,1); // deletes the first item

todos.splice(1,1); // deletes the second item

todos.splice(2,1); // deletes the third item

